import OpenAI from "openai";
export default async function(req,res){if(req.method!=="POST")return res.status(405).json({error:"Method not allowed"});if(!process.env.OPENAI_API_KEY)return res.status(503).json({error:"OPENAI_API_KEY غير موجود في Vercel."});try{let {messages=[],products=[]}=req.body||{},c=new OpenAI({apiKey:process.env.OPENAI_API_KEY}),system=`أنت FITARO AI، مساعد أزياء متخصص في البدلات الموجودة على FITARO. ساعد في المقاس والمناسبة واللون والـFit، واشرح Slim Fit وModern Fit وRegular Fit. تحدث بالعربية المصرية البسيطة والراقية. استخدم المنتجات المتاحة فقط عند ترشيح منتج. المقاسات تقديرية. إذا أراد تجربة بدلة وجّهه لرفع الصورة واختيار البدلة ثم الضغط على جرّب البدلة. المنتجات: ${JSON.stringify(products)}`,r=await c.chat.completions.create({model:process.env.OPENAI_MODEL||"gpt-4o-mini",messages:[{role:"system",content:system},...messages.slice(-12)]});res.json({text:r.choices[0].message.content})}catch(e){res.status(500).json({error:e.message||"Chat error"})}}